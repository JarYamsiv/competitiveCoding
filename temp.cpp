vector<int> climbingLeaderboard(vector<int> scores, vector<int> alice) 
{
    int alice_i=alice.size();
    int scores_i=0;
    vector<int> alice_pos;
    
    while(1)
    {
        if(scores_i>scores.size())
        {
            break;
        }
        if(alice_i<0)
        {
            break;
        }
    }

}